# 卡片序號，從發行後台取得
VC_SERNUM=YOUR_VC_SERNUM

# 卡片樣板代號，從發行後台取得
VC_UID=YOUR_VC_UID

# 發行者存取權杖，從發行後台取得
ISSUER_ACCESS_TOKEN=YOUR_ISSUER_ACCESS_TOKEN

# 驗證器參考碼
VERIFIER_REF=YOUR_VERIFIER_REF

# 驗證器存取權杖
VERIFIER_ACCESS_TOKEN=YOUR_VERIFIER_ACCESS_TOKEN